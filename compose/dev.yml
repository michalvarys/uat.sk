# Dev — doplňuje base.yml.
#
# Nejvolnější prostředí: porty ven i pro databázi.

services:
  postgresql:
    ports:
      - "127.0.0.1:${POSTGRESQL_PORT:-5433}:5432"

  strapi:
    environment:
      NODE_ENV: development

  frontend:
    environment:
      NODE_ENV: production
